services:
  backend:
    build: ./backend
    ports:
      - "6767:6767"
    volumes:
      - ./artifacts:/app/artifacts
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
